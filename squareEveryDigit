function squareDigits(num){
  var numString = num.toString();
  var numArray = numString.split('');
  var squaredArray = [];
  for (var i = 0; i < numArray.length; i++) {
    squaredArray.push(Math.pow(numArray[i], 2));
  }
  return parseInt(squaredArray.join(''));
}
